// TODO(T5): Walk the AX tree via AXorcist, returning DetectedElement arrays.
// Use AXorcist's AXElement API instead of raw AXUIElementCopyAttributeValue calls.

import Foundation

internal enum ElementDetectionService {
    // Placeholder — populated in T5.
}
