// TODO(T6): Manage in-memory snapshot store: create, retrieve, list, and evict snapshots.
// Snapshots pair a DetectedElement tree with a captured screenshot path.

import Foundation

internal enum SnapshotManager {
    // Placeholder — populated in T6.
}
