// TODO(T5): Render Set-of-Mark (SoM) annotations onto an NSImage — numbered bounding boxes
// overlaid on a screenshot to let the LLM reference UI elements by index.

import Foundation

internal enum AnnotatedScreenshot {
    // Placeholder — populated in T5.
}
