// TODO(T4): Route decoded Request objects to the appropriate service handler.
// Should dispatch based on OpName and return typed Response values.

import Foundation

internal enum Dispatcher {
    // Placeholder — populated in T4 (swift-daemon-host task).
}
